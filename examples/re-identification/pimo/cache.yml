version: "1"
seed: 42
masking:
  - selector:
      jsonpath: "location"
    mask:
      incremental:
        start: 1
        increment: 1
    cache: "cacheLocation"

caches:
  cacheLocation:
    unique: false
